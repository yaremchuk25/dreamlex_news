{% extends '::base.html.twig' %}

{% block body -%}
    <h1 class="blog-post-title"><a href="{{ path('_show', { 'id': entity.id}) }}">{{ entity.title }}</a></h1>
    <p class="blog-post-meta">{{ entity.date }}</p>
    <p>{{ entity.text }}</p>
    <a class="btn || btn-primary" href="{{ path('_edit', { 'id': entity.id }) }}">Edit</a>
    <div style="margin-top: 1rem;">{{ form(delete_form) }}</div>
    {#<table class="record_properties">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Title</th>
                <td>{{ entity.title }}</td>
            </tr>
            <tr>
                <th>Text</th>
                <td>{{ entity.text }}</td>
            </tr>
            <tr>
                <th>Date</th>
                <td>{{ entity.date }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>#}
{% endblock %}
